/** Total number of CVEs in the dataset used for progress calculation and display. */
export const DATASET_VULN_TOTAL = 236656;

/** Human-readable display labels for raw kaiStatus values. */
export const KAI_STATUS_LABEL: Record<string, string> = {
  'invalid - norisk': 'Manual Clear',
  'ai-invalid-norisk': 'AI Clear',
};
